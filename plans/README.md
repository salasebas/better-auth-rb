# Implementation Plans

Index for active improvement plans. Each executor must read the assigned plan
fully before starting, honor its STOP conditions, and update the status row when
done.

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED.

## OAuth Provider Parity (019, 025)

| Plan | Title | Depends on | Status |
| --- | --- | --- | --- |
| 019 | OAuth provider unified parity | - | DONE |
| 025 | OAuth provider parity gaps, JWT requirement, tests | 019 | DONE |

Recommended order: **019 (done) → 025**.

## Other Active Plans

| Plan | Title | Depends on | Status |
| --- | --- | --- | --- |
| 019 | Core i18n plugin (`BetterAuth::Plugins.i18n`) | - | DONE |
| 026 | Upstream docs deprecation audit & skip-legacy verification | - | TODO |

## Docs Parity Initiative (020–025)

Upstream Better Auth v1.6.9 docs -> RubyAuth copy-first port under
`docs-site/content/docs/`. Reconciled on 2026-06-16 against commit `2ce7a4a`.

The executor must treat the upstream MDX as the source of truth: literally copy
and paste the most modern upstream doc content available for the current target
under `reference/upstream-src/1.6.9/repository/docs/content/docs/`, keep the
same prose, headings, tables, callouts, and section order, and only adapt code
examples, install commands, package names, and Ruby-specific notes. If examples
render badly after the copy, fix the formatting; do not rewrite the surrounding
documentation as a summary.

Current audit: concepts, adapters, authentication provider pages, and several
plugin pages already exist. The remaining high-value gaps are mostly plugins,
reference/guides, `reference/resources.mdx`, and a generated database schema
and endpoint index. Unsupported or intentionally excluded docs must not be
presented as supported: `plugins/mcp`, upstream `plugins/oidc-provider`,
`plugins/test-utils`, client-only pages, JS framework integrations, upstream
infrastructure, and unported payment plugins other than Stripe.

| Plan | Title | Depends on | Status |
| --- | --- | --- | --- |
| 020 | Docs parity foundation (tooling + manifest) | - | TODO |
| 021 | Concepts & getting started finish-pass | 020 | TODO |
| 022 | Supported plugins copy-first parity | 020, 021 (rec.) | TODO |
| 023 | Adapters & authentication finish-pass | 020 | TODO |
| 024 | Reference, guides & errors | 020-023 | TODO |
| 025 | Generated database schemas and endpoint resources | 020, 022 | TODO |

Recommended order if resumed: **026 → 020 → 021 → 023 → 022 → 025 → 024**.
Plan 026 produces the deprecation matrix for docs work. Docs plan 025 can start
after 020 only if the supported plugin list from 022 is manually finalized
first; otherwise wait for 022.

## Notes For Executors

- Do not modify `reference/upstream-src/**`.
- OAuth plan **025** (`025-oauth-provider-parity-gaps-and-tests.md`) is done; it
  explicitly excluded `docs-site/` except the oauth-provider gem README.
- Do not mark docs parity plans DONE without explicit maintainer approval.
- Keep core tests in Minitest under `packages/better_auth/test`.
- Do not use `mcp` or upstream `oidc-provider` as supported docs targets.
- Prefer generator output from RubyAuth (`BetterAuth::Schema`,
  `BetterAuth::Schema::SQL`, and OpenAPI metadata) over manually mapping tables,
  fields, or endpoints.

## Findings considered and rejected

- **Auto-enable JWT plugin when oauth-provider loads**: upstream and maintainer
  intent require explicit registration; init must fail with `jwt_config` instead.
- **Full `sector_identifier_uri` implementation**: upstream v1.6.9 also rejects
  multi-host pairwise without it; message alignment only in oauth plan 025.
- **docs-site Support Boundary refresh**: deferred per maintainer; oauth plan
  025 excluded docs-site entirely.

Generated by improve skill on 2026-06-16 (oauth plan 025 at commit `2ce7a4a`).
