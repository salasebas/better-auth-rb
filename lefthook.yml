# frozen_string_literal: true
# Lefthook configuration - format staged Ruby before commit
# Install with: bundle exec lefthook install

output:
  - success
  - failure

pre-commit:
  parallel: false
  commands:
    format:
      glob: "*.rb"
      run: bundle exec standardrb --fix {staged_files}
      stage_fixed: true
