---
title: Database Schemas
description: Generated RubyAuth database tables and SQL for supported plugins.
---

<RubyAuthDisclaimer />

This page is generated by `docs-site/scripts/generate-reference-resources.rb`.
Do not edit the generated schema reference by hand; update the Ruby source of truth and rerun the generator.

Generated configuration: supported Ruby plugin set, email/password auth, database-backed rate limiting, and optional schema-bearing supported plugin features. Actual app schemas and endpoints may differ when plugins are disabled or names and fields are customized.

Generated count: 24 tables.

The table metadata below comes from `BetterAuth::Schema.auth_tables(auth.context.options)`. The SQL sections come from `BetterAuth::Schema::SQL.create_statements(auth.context.options, dialect: dialect)`.

## Tables

### user

- Logical model key: `user`
- Physical table/model name: `users`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `displayUsername` | `display_username` | string | no |  |  |  |  |  |  |
| `email` | `email` | string | yes | yes |  |  |  |  |  |
| `emailVerified` | `email_verified` | boolean | yes |  |  |  | no |  | no |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `image` | `image` | string | no |  |  |  |  |  |  |
| `isAnonymous` | `is_anonymous` | boolean | no |  |  |  | no |  | no |
| `lastLoginMethod` | `lastLoginMethod` | string | no |  |  |  |  |  | no |
| `name` | `name` | string | yes |  |  |  |  |  |  |
| `phoneNumber` | `phone_number` | string | no | yes |  |  |  | yes |  |
| `phoneNumberVerified` | `phone_number_verified` | boolean | no |  |  |  |  | yes | no |
| `stripeCustomerId` | `stripe_customer_id` | string | no |  |  |  |  |  |  |
| `twoFactorEnabled` | `two_factor_enabled` | boolean | no |  |  |  | no | yes |  |
| `updatedAt` | `updated_at` | date | yes |  |  |  | generated |  |  |
| `username` | `username` | string | no | yes |  |  |  | yes |  |

### session

- Logical model key: `session`
- Physical table/model name: `sessions`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `activeOrganizationId` | `active_organization_id` | string | no |  |  |  |  |  |  |
| `activeTeamId` | `active_team_id` | string | no |  |  |  |  |  |  |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `expiresAt` | `expires_at` | date | yes |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `ipAddress` | `ip_address` | string | no |  |  |  |  |  |  |
| `token` | `token` | string | yes | yes |  |  |  |  |  |
| `updatedAt` | `updated_at` | date | yes |  |  |  | generated |  |  |
| `userAgent` | `user_agent` | string | no |  |  |  |  |  |  |
| `userId` | `user_id` | string | yes |  | yes | users.id, on delete cascade |  |  |  |

### account

- Logical model key: `account`
- Physical table/model name: `accounts`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `accessToken` | `access_token` | string | no |  |  |  |  | no |  |
| `accessTokenExpiresAt` | `access_token_expires_at` | date | no |  |  |  |  | no |  |
| `accountId` | `account_id` | string | yes |  |  |  |  |  |  |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `idToken` | `id_token` | string | no |  |  |  |  | no |  |
| `password` | `password` | string | no |  |  |  |  | no |  |
| `providerId` | `provider_id` | string | yes |  |  |  |  |  |  |
| `refreshToken` | `refresh_token` | string | no |  |  |  |  | no |  |
| `refreshTokenExpiresAt` | `refresh_token_expires_at` | date | no |  |  |  |  | no |  |
| `scope` | `scope` | string | no |  |  |  |  |  |  |
| `updatedAt` | `updated_at` | date | yes |  |  |  | generated |  |  |
| `userId` | `user_id` | string | yes |  | yes | users.id, on delete cascade |  |  |  |

### verification

- Logical model key: `verification`
- Physical table/model name: `verifications`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `expiresAt` | `expires_at` | date | yes |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `identifier` | `identifier` | string | yes |  | yes |  |  |  |  |
| `updatedAt` | `updated_at` | date | yes |  |  |  | generated |  |  |
| `value` | `value` | string | yes |  |  |  |  |  |  |

### apikey

- Logical model key: `apikey`
- Physical table/model name: `api_keys`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `configId` | `config_id` | string | yes |  | yes |  | default |  |  |
| `createdAt` | `created_at` | date | yes |  |  |  |  |  |  |
| `enabled` | `enabled` | boolean | no |  |  |  | yes |  |  |
| `expiresAt` | `expires_at` | date | no |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `key` | `key` | string | yes |  | yes |  |  |  |  |
| `lastRefillAt` | `last_refill_at` | date | no |  |  |  |  |  |  |
| `lastRequest` | `last_request` | date | no |  |  |  |  |  |  |
| `metadata` | `metadata` | string | no |  |  |  |  |  |  |
| `name` | `name` | string | no |  |  |  |  |  |  |
| `permissions` | `permissions` | string | no |  |  |  |  |  |  |
| `prefix` | `prefix` | string | no |  |  |  |  |  |  |
| `rateLimitEnabled` | `rate_limit_enabled` | boolean | no |  |  |  | yes |  |  |
| `rateLimitMax` | `rate_limit_max` | number | no |  |  |  | 10 |  |  |
| `rateLimitTimeWindow` | `rate_limit_time_window` | number | no |  |  |  | 86400000 |  |  |
| `referenceId` | `reference_id` | string | yes |  | yes |  |  |  |  |
| `refillAmount` | `refill_amount` | number | no |  |  |  |  |  |  |
| `refillInterval` | `refill_interval` | number | no |  |  |  |  |  |  |
| `remaining` | `remaining` | number | no |  |  |  |  |  |  |
| `requestCount` | `request_count` | number | no |  |  |  | 0 |  |  |
| `start` | `start` | string | no |  |  |  |  |  |  |
| `updatedAt` | `updated_at` | date | yes |  |  |  |  |  |  |

### deviceCode

- Logical model key: `deviceCode`
- Physical table/model name: `device_codes`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `clientId` | `client_id` | string | no |  |  |  |  |  |  |
| `deviceCode` | `device_code` | string | yes |  |  |  |  |  |  |
| `expiresAt` | `expires_at` | date | yes |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `lastPolledAt` | `last_polled_at` | date | no |  |  |  |  |  |  |
| `pollingInterval` | `polling_interval` | number | no |  |  |  |  |  |  |
| `scope` | `scope` | string | no |  |  |  |  |  |  |
| `status` | `status` | string | yes |  |  |  |  |  |  |
| `userCode` | `user_code` | string | yes |  |  |  |  |  |  |
| `userId` | `user_id` | string | no |  |  |  |  |  |  |

### invitation

- Logical model key: `invitation`
- Physical table/model name: `invitations`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `email` | `email` | string | yes |  | yes |  |  |  |  |
| `expiresAt` | `expires_at` | date | yes |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `inviterId` | `inviter_id` | string | yes |  |  | user.id, on delete cascade |  |  |  |
| `organizationId` | `organization_id` | string | yes |  | yes | organization.id, on delete cascade |  |  |  |
| `role` | `role` | string | yes |  |  |  |  |  |  |
| `status` | `status` | string | yes |  |  |  | pending |  |  |
| `teamId` | `team_id` | string | no |  |  |  |  |  |  |

### jwks

- Logical model key: `jwks`
- Physical table/model name: `jwks`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `alg` | `alg` | string | no |  |  |  |  |  |  |
| `createdAt` | `created_at` | date | yes |  |  |  |  |  |  |
| `crv` | `crv` | string | no |  |  |  |  |  |  |
| `e` | `e` | string | no |  |  |  |  |  |  |
| `expiresAt` | `expires_at` | date | no |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `kty` | `kty` | string | no |  |  |  |  |  |  |
| `n` | `n` | string | no |  |  |  |  |  |  |
| `pem` | `pem` | string | no |  |  |  |  |  |  |
| `privateKey` | `private_key` | string | yes |  |  |  |  |  |  |
| `publicKey` | `public_key` | string | yes |  |  |  |  |  |  |
| `x` | `x` | string | no |  |  |  |  |  |  |
| `y` | `y` | string | no |  |  |  |  |  |  |

### member

- Logical model key: `member`
- Physical table/model name: `members`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `organizationId` | `organization_id` | string | yes |  | yes | organization.id, on delete cascade |  |  |  |
| `role` | `role` | string | yes |  |  |  | member |  |  |
| `userId` | `user_id` | string | yes |  | yes | user.id, on delete cascade |  |  |  |

### oauthAccessToken

- Logical model key: `oauthAccessToken`
- Physical table/model name: `oauth_access_tokens`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `authTime` | `auth_time` | date | no |  |  |  |  |  |  |
| `clientId` | `client_id` | string | yes |  | yes | oauthClient.clientId, on delete cascade |  |  |  |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `expiresAt` | `expires_at` | date | yes |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `referenceId` | `reference_id` | string | no |  |  |  |  |  |  |
| `refreshId` | `refresh_id` | string | no |  | yes | oauthRefreshToken.id, on delete cascade |  |  |  |
| `revoked` | `revoked` | date | no |  |  |  |  |  |  |
| `scopes` | `scopes` | string[] | yes |  |  |  |  |  |  |
| `sessionId` | `session_id` | string | no |  | yes | session.id, on delete set null |  |  |  |
| `token` | `token` | string | yes | yes |  |  |  |  |  |
| `updatedAt` | `updated_at` | date | yes |  |  |  | generated |  |  |
| `userId` | `user_id` | string | no |  | yes | user.id, on delete cascade |  |  |  |

### oauthClient

- Logical model key: `oauthClient`
- Physical table/model name: `oauth_clients`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `clientId` | `client_id` | string | yes | yes |  |  |  |  |  |
| `clientSecret` | `client_secret` | string | no |  |  |  |  |  |  |
| `clientSecretExpiresAt` | `client_secret_expires_at` | number | no |  |  |  |  |  |  |
| `contacts` | `contacts` | string[] | no |  |  |  |  |  |  |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `disabled` | `disabled` | boolean | no |  |  |  | no |  |  |
| `enableEndSession` | `enable_end_session` | boolean | no |  |  |  |  |  |  |
| `grantTypes` | `grant_types` | string[] | no |  |  |  |  |  |  |
| `icon` | `icon` | string | no |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `metadata` | `metadata` | json | no |  |  |  |  |  |  |
| `name` | `name` | string | no |  |  |  |  |  |  |
| `policy` | `policy` | string | no |  |  |  |  |  |  |
| `postLogoutRedirectUris` | `post_logout_redirect_uris` | string[] | no |  |  |  |  |  |  |
| `public` | `public` | boolean | no |  |  |  |  |  |  |
| `redirectUris` | `redirect_uris` | string[] | yes |  |  |  |  |  |  |
| `referenceId` | `reference_id` | string | no |  | yes |  |  |  |  |
| `requirePkce` | `require_pkce` | boolean | no |  |  |  |  |  |  |
| `responseTypes` | `response_types` | string[] | no |  |  |  |  |  |  |
| `scopes` | `scopes` | string[] | no |  |  |  |  |  |  |
| `skipConsent` | `skip_consent` | boolean | no |  |  |  |  |  |  |
| `softwareId` | `software_id` | string | no |  |  |  |  |  |  |
| `softwareStatement` | `software_statement` | string | no |  |  |  |  |  |  |
| `softwareVersion` | `software_version` | string | no |  |  |  |  |  |  |
| `subjectType` | `subject_type` | string | no |  |  |  |  |  |  |
| `tokenEndpointAuthMethod` | `token_endpoint_auth_method` | string | no |  |  |  |  |  |  |
| `tos` | `tos` | string | no |  |  |  |  |  |  |
| `type` | `type` | string | no |  |  |  |  |  |  |
| `updatedAt` | `updated_at` | date | yes |  |  |  | generated |  |  |
| `uri` | `uri` | string | no |  |  |  |  |  |  |
| `userId` | `user_id` | string | no |  | yes | user.id, on delete cascade |  |  |  |

### oauthConsent

- Logical model key: `oauthConsent`
- Physical table/model name: `oauth_consents`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `clientId` | `client_id` | string | yes |  | yes | oauthClient.clientId, on delete cascade |  |  |  |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `referenceId` | `reference_id` | string | no |  | yes |  |  |  |  |
| `scopes` | `scopes` | string[] | yes |  |  |  |  |  |  |
| `updatedAt` | `updated_at` | date | yes |  |  |  | generated |  |  |
| `userId` | `user_id` | string | no |  | yes | user.id, on delete cascade |  |  |  |

### oauthRefreshToken

- Logical model key: `oauthRefreshToken`
- Physical table/model name: `oauth_refresh_tokens`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `authTime` | `auth_time` | date | no |  |  |  |  |  |  |
| `clientId` | `client_id` | string | yes |  | yes | oauthClient.clientId, on delete cascade |  |  |  |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `expiresAt` | `expires_at` | date | no |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `referenceId` | `reference_id` | string | no |  | yes |  |  |  |  |
| `revoked` | `revoked` | date | no |  |  |  |  |  |  |
| `scopes` | `scopes` | string[] | yes |  |  |  |  |  |  |
| `sessionId` | `session_id` | string | no |  | yes | session.id, on delete set null |  |  |  |
| `token` | `token` | string | yes |  |  |  |  |  |  |
| `userId` | `user_id` | string | no |  | yes | user.id, on delete cascade |  |  |  |

### organization

- Logical model key: `organization`
- Physical table/model name: `organizations`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `logo` | `logo` | string | no |  |  |  |  |  |  |
| `metadata` | `metadata` | string | no |  |  |  |  |  |  |
| `name` | `name` | string | yes |  |  |  |  |  |  |
| `slug` | `slug` | string | yes | yes | yes |  |  |  |  |
| `stripeCustomerId` | `stripe_customer_id` | string | no |  |  |  |  |  |  |
| `updatedAt` | `updated_at` | date | no |  |  |  |  |  |  |

### organizationRole

- Logical model key: `organizationRole`
- Physical table/model name: `organization_roles`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `organizationId` | `organization_id` | string | yes |  | yes | organization.id, on delete cascade |  |  |  |
| `permission` | `permission` | string | yes |  |  |  |  |  |  |
| `role` | `role` | string | yes |  | yes |  |  |  |  |
| `updatedAt` | `updated_at` | date | no |  |  |  |  |  |  |

### passkey

- Logical model key: `passkey`
- Physical table/model name: `passkeys`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `aaguid` | `aaguid` | string | no |  |  |  |  |  |  |
| `backedUp` | `backed_up` | boolean | yes |  |  |  |  |  |  |
| `counter` | `counter` | number | yes |  |  |  |  |  |  |
| `createdAt` | `created_at` | date | no |  |  |  |  |  |  |
| `credentialId` | `credential_id` | string | yes | yes |  |  |  |  |  |
| `deviceType` | `device_type` | string | yes |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `name` | `name` | string | no |  |  |  |  |  |  |
| `publicKey` | `public_key` | string | yes |  |  |  |  |  |  |
| `transports` | `transports` | string | no |  |  |  |  |  |  |
| `userId` | `user_id` | string | yes |  | yes | user.id, on delete cascade |  |  |  |

### rateLimit

- Logical model key: `rateLimit`
- Physical table/model name: `rate_limits`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `count` | `count` | number | yes |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `key` | `key` | string | yes | yes |  |  |  |  |  |
| `lastRequest` | `last_request` | number | yes |  |  |  | generated |  |  |

### scimProvider

- Logical model key: `scimProvider`
- Physical table/model name: `scim_providers`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `organizationId` | `organization_id` | string | no |  |  |  |  |  |  |
| `providerId` | `provider_id` | string | yes | yes |  |  |  |  |  |
| `scimToken` | `scim_token` | string | yes | yes |  |  |  |  |  |
| `userId` | `user_id` | string | no |  |  |  |  |  |  |

### ssoProvider

- Logical model key: `ssoProvider`
- Physical table/model name: `sso_providers`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `domain` | `domain` | string | yes |  |  |  |  |  |  |
| `domainVerified` | `domain_verified` | boolean | no |  |  |  | no |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `issuer` | `issuer` | string | yes |  |  |  |  |  |  |
| `oidcConfig` | `oidc_config` | string | no |  |  |  |  |  |  |
| `organizationId` | `organization_id` | string | no |  |  |  |  |  |  |
| `providerId` | `provider_id` | string | yes | yes |  |  |  |  |  |
| `samlConfig` | `saml_config` | string | no |  |  |  |  |  |  |
| `userId` | `user_id` | string | yes |  |  | user.id, on delete cascade |  |  |  |

### subscription

- Logical model key: `subscription`
- Physical table/model name: `subscriptions`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `billingInterval` | `billing_interval` | string | no |  |  |  |  |  |  |
| `cancelAt` | `cancel_at` | date | no |  |  |  |  |  |  |
| `cancelAtPeriodEnd` | `cancel_at_period_end` | boolean | no |  |  |  | no |  |  |
| `canceledAt` | `canceled_at` | date | no |  |  |  |  |  |  |
| `endedAt` | `ended_at` | date | no |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `limits` | `limits` | json | no |  |  |  |  |  |  |
| `periodEnd` | `period_end` | date | no |  |  |  |  |  |  |
| `periodStart` | `period_start` | date | no |  |  |  |  |  |  |
| `plan` | `plan` | string | yes |  |  |  |  |  |  |
| `referenceId` | `reference_id` | string | yes |  |  |  |  |  |  |
| `seats` | `seats` | number | no |  |  |  |  |  |  |
| `status` | `status` | string | no |  |  |  | incomplete |  |  |
| `stripeCustomerId` | `stripe_customer_id` | string | no |  |  |  |  |  |  |
| `stripeScheduleId` | `stripe_schedule_id` | string | no |  |  |  |  |  |  |
| `stripeSubscriptionId` | `stripe_subscription_id` | string | no |  |  |  |  |  |  |
| `trialEnd` | `trial_end` | date | no |  |  |  |  |  |  |
| `trialStart` | `trial_start` | date | no |  |  |  |  |  |  |

### team

- Logical model key: `team`
- Physical table/model name: `teams`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `createdAt` | `created_at` | date | yes |  |  |  | generated |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `name` | `name` | string | yes |  |  |  |  |  |  |
| `organizationId` | `organization_id` | string | yes |  | yes | organization.id, on delete cascade |  |  |  |
| `updatedAt` | `updated_at` | date | no |  |  |  |  |  |  |

### teamMember

- Logical model key: `teamMember`
- Physical table/model name: `team_members`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `createdAt` | `created_at` | date | no |  |  |  | generated |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `teamId` | `team_id` | string | yes |  | yes | team.id, on delete cascade |  |  |  |
| `userId` | `user_id` | string | yes |  | yes | user.id, on delete cascade |  |  |  |

### twoFactor

- Logical model key: `twoFactor`
- Physical table/model name: `two_factors`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `backupCodes` | `backup_codes` | string | yes |  |  |  |  | no |  |
| `failedVerificationCount` | `failed_verification_count` | number | no |  |  |  | 0 | no | no |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `lockedUntil` | `locked_until` | date | no |  |  |  |  | no | no |
| `secret` | `secret` | string | yes |  | yes |  |  | no |  |
| `userId` | `user_id` | string | yes |  | yes | user.id, on delete cascade |  | no |  |
| `verified` | `verified` | boolean | no |  |  |  | yes |  | no |

### walletAddress

- Logical model key: `walletAddress`
- Physical table/model name: `wallet_addresses`

| Logical field | Physical field | Type | Required | Unique | Index | Reference | Default | Returned | Input |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `address` | `address` | string | yes |  |  |  |  |  |  |
| `chainId` | `chain_id` | number | yes |  |  |  |  |  |  |
| `createdAt` | `created_at` | date | yes |  |  |  |  |  |  |
| `id` | `id` | string | yes |  |  |  |  |  |  |
| `isPrimary` | `is_primary` | boolean |  |  |  |  | no |  |  |
| `userId` | `user_id` | string | yes |  | yes | user.id, on delete cascade |  |  |  |

## SQL Creation Statements

### postgres

```sql
CREATE TABLE IF NOT EXISTS "users" (
  "id" text PRIMARY KEY NOT NULL,
  "name" text NOT NULL,
  "email" text NOT NULL,
  "email_verified" boolean NOT NULL DEFAULT false,
  "image" text,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "username" text,
  "display_username" text,
  "is_anonymous" boolean DEFAULT false,
  "phone_number" text,
  "phone_number_verified" boolean,
  "two_factor_enabled" boolean DEFAULT false,
  "lastLoginMethod" text,
  "stripe_customer_id" text,
  UNIQUE ("email"),
  UNIQUE ("username"),
  UNIQUE ("phone_number")
);

CREATE TABLE IF NOT EXISTS "sessions" (
  "id" text PRIMARY KEY NOT NULL,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "expires_at" timestamptz NOT NULL,
  "token" text NOT NULL,
  "ip_address" text,
  "user_agent" text,
  "user_id" text NOT NULL,
  "active_organization_id" text,
  "active_team_id" text,
  UNIQUE ("token"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "accounts" (
  "id" text PRIMARY KEY NOT NULL,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "account_id" text NOT NULL,
  "provider_id" text NOT NULL,
  "user_id" text NOT NULL,
  "access_token" text,
  "refresh_token" text,
  "id_token" text,
  "access_token_expires_at" timestamptz,
  "refresh_token_expires_at" timestamptz,
  "scope" text,
  "password" text,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "verifications" (
  "id" text PRIMARY KEY NOT NULL,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "identifier" text NOT NULL,
  "value" text NOT NULL,
  "expires_at" timestamptz NOT NULL
);

CREATE TABLE IF NOT EXISTS "two_factors" (
  "id" text PRIMARY KEY NOT NULL,
  "secret" text NOT NULL,
  "backup_codes" text NOT NULL,
  "user_id" text NOT NULL,
  "verified" boolean DEFAULT true,
  "failed_verification_count" integer DEFAULT 0,
  "locked_until" timestamptz,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "organizations" (
  "id" text PRIMARY KEY NOT NULL,
  "name" text NOT NULL,
  "slug" text NOT NULL,
  "logo" text,
  "metadata" text,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz,
  "stripe_customer_id" text,
  UNIQUE ("slug")
);

CREATE TABLE IF NOT EXISTS "members" (
  "id" text PRIMARY KEY NOT NULL,
  "organization_id" text NOT NULL,
  "user_id" text NOT NULL,
  "role" text NOT NULL DEFAULT 'member',
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY ("organization_id") REFERENCES "organizations" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "invitations" (
  "id" text PRIMARY KEY NOT NULL,
  "organization_id" text NOT NULL,
  "email" text NOT NULL,
  "role" text NOT NULL,
  "status" text NOT NULL DEFAULT 'pending',
  "expires_at" timestamptz NOT NULL,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "inviter_id" text NOT NULL,
  "team_id" text,
  FOREIGN KEY ("organization_id") REFERENCES "organizations" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("inviter_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "teams" (
  "id" text PRIMARY KEY NOT NULL,
  "name" text NOT NULL,
  "organization_id" text NOT NULL,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz,
  FOREIGN KEY ("organization_id") REFERENCES "organizations" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "team_members" (
  "id" text PRIMARY KEY NOT NULL,
  "team_id" text NOT NULL,
  "user_id" text NOT NULL,
  "created_at" timestamptz DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY ("team_id") REFERENCES "teams" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "organization_roles" (
  "id" text PRIMARY KEY NOT NULL,
  "organization_id" text NOT NULL,
  "role" text NOT NULL,
  "permission" text NOT NULL,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz,
  FOREIGN KEY ("organization_id") REFERENCES "organizations" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "jwks" (
  "id" text PRIMARY KEY NOT NULL,
  "public_key" text NOT NULL,
  "private_key" text NOT NULL,
  "created_at" timestamptz NOT NULL,
  "expires_at" timestamptz,
  "alg" text,
  "kty" text,
  "crv" text,
  "x" text,
  "y" text,
  "pem" text,
  "n" text,
  "e" text
);

CREATE TABLE IF NOT EXISTS "device_codes" (
  "id" text PRIMARY KEY NOT NULL,
  "device_code" text NOT NULL,
  "user_code" text NOT NULL,
  "user_id" text,
  "expires_at" timestamptz NOT NULL,
  "status" text NOT NULL,
  "last_polled_at" timestamptz,
  "polling_interval" integer,
  "client_id" text,
  "scope" text
);

CREATE TABLE IF NOT EXISTS "wallet_addresses" (
  "id" text PRIMARY KEY NOT NULL,
  "user_id" text NOT NULL,
  "address" text NOT NULL,
  "chain_id" integer NOT NULL,
  "is_primary" boolean DEFAULT false,
  "created_at" timestamptz NOT NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "api_keys" (
  "id" text PRIMARY KEY NOT NULL,
  "config_id" text NOT NULL DEFAULT 'default',
  "name" text,
  "start" text,
  "prefix" text,
  "key" text NOT NULL,
  "reference_id" text NOT NULL,
  "refill_interval" integer,
  "refill_amount" integer,
  "last_refill_at" timestamptz,
  "enabled" boolean DEFAULT true,
  "rate_limit_enabled" boolean DEFAULT true,
  "rate_limit_time_window" integer DEFAULT 86400000,
  "rate_limit_max" integer DEFAULT 10,
  "request_count" integer DEFAULT 0,
  "remaining" integer,
  "last_request" timestamptz,
  "expires_at" timestamptz,
  "created_at" timestamptz NOT NULL,
  "updated_at" timestamptz NOT NULL,
  "permissions" text,
  "metadata" text
);

CREATE TABLE IF NOT EXISTS "passkeys" (
  "id" text PRIMARY KEY NOT NULL,
  "name" text,
  "public_key" text NOT NULL,
  "user_id" text NOT NULL,
  "credential_id" text NOT NULL,
  "counter" integer NOT NULL,
  "device_type" text NOT NULL,
  "backed_up" boolean NOT NULL,
  "transports" text,
  "created_at" timestamptz,
  "aaguid" text,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE,
  UNIQUE ("credential_id")
);

CREATE TABLE IF NOT EXISTS "oauth_clients" (
  "id" text PRIMARY KEY NOT NULL,
  "client_id" text NOT NULL,
  "client_secret" text,
  "disabled" boolean DEFAULT false,
  "skip_consent" boolean,
  "enable_end_session" boolean,
  "client_secret_expires_at" integer,
  "scopes" jsonb,
  "user_id" text,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "name" text,
  "uri" text,
  "icon" text,
  "contacts" jsonb,
  "tos" text,
  "policy" text,
  "software_id" text,
  "software_version" text,
  "software_statement" text,
  "redirect_uris" jsonb NOT NULL,
  "post_logout_redirect_uris" jsonb,
  "token_endpoint_auth_method" text,
  "grant_types" jsonb,
  "response_types" jsonb,
  "public" boolean,
  "type" text,
  "require_pkce" boolean,
  "subject_type" text,
  "reference_id" text,
  "metadata" jsonb,
  UNIQUE ("client_id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "oauth_refresh_tokens" (
  "id" text PRIMARY KEY NOT NULL,
  "token" text NOT NULL,
  "client_id" text NOT NULL,
  "session_id" text,
  "user_id" text,
  "reference_id" text,
  "auth_time" timestamptz,
  "expires_at" timestamptz,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "revoked" timestamptz,
  "scopes" jsonb NOT NULL,
  FOREIGN KEY ("client_id") REFERENCES "oauth_clients" ("client_id") ON DELETE CASCADE,
  FOREIGN KEY ("session_id") REFERENCES "sessions" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "oauth_access_tokens" (
  "id" text PRIMARY KEY NOT NULL,
  "token" text NOT NULL,
  "expires_at" timestamptz NOT NULL,
  "client_id" text NOT NULL,
  "user_id" text,
  "session_id" text,
  "scopes" jsonb NOT NULL,
  "revoked" timestamptz,
  "reference_id" text,
  "auth_time" timestamptz,
  "refresh_id" text,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE ("token"),
  FOREIGN KEY ("client_id") REFERENCES "oauth_clients" ("client_id") ON DELETE CASCADE,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("session_id") REFERENCES "sessions" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("refresh_id") REFERENCES "oauth_refresh_tokens" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "oauth_consents" (
  "id" text PRIMARY KEY NOT NULL,
  "client_id" text NOT NULL,
  "user_id" text,
  "reference_id" text,
  "scopes" jsonb NOT NULL,
  "created_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY ("client_id") REFERENCES "oauth_clients" ("client_id") ON DELETE CASCADE,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "scim_providers" (
  "id" text PRIMARY KEY NOT NULL,
  "provider_id" text NOT NULL,
  "scim_token" text NOT NULL,
  "organization_id" text,
  "user_id" text,
  UNIQUE ("provider_id"),
  UNIQUE ("scim_token")
);

CREATE TABLE IF NOT EXISTS "sso_providers" (
  "id" text PRIMARY KEY NOT NULL,
  "issuer" text NOT NULL,
  "oidc_config" text,
  "saml_config" text,
  "user_id" text NOT NULL,
  "provider_id" text NOT NULL,
  "domain" text NOT NULL,
  "organization_id" text,
  "domain_verified" boolean DEFAULT false,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE,
  UNIQUE ("provider_id")
);

CREATE TABLE IF NOT EXISTS "subscriptions" (
  "id" text PRIMARY KEY NOT NULL,
  "plan" text NOT NULL,
  "reference_id" text NOT NULL,
  "stripe_customer_id" text,
  "stripe_subscription_id" text,
  "status" text DEFAULT 'incomplete',
  "period_start" timestamptz,
  "period_end" timestamptz,
  "trial_start" timestamptz,
  "trial_end" timestamptz,
  "cancel_at_period_end" boolean DEFAULT false,
  "cancel_at" timestamptz,
  "canceled_at" timestamptz,
  "ended_at" timestamptz,
  "seats" integer,
  "billing_interval" text,
  "stripe_schedule_id" text,
  "limits" jsonb
);

CREATE TABLE IF NOT EXISTS "rate_limits" (
  "id" text PRIMARY KEY NOT NULL,
  "key" text NOT NULL,
  "count" integer NOT NULL,
  "last_request" bigint NOT NULL,
  UNIQUE ("key")
);

CREATE INDEX IF NOT EXISTS "index_sessions_on_user_id" ON "sessions" ("user_id");

CREATE INDEX IF NOT EXISTS "index_accounts_on_user_id" ON "accounts" ("user_id");

CREATE INDEX IF NOT EXISTS "index_verifications_on_identifier" ON "verifications" ("identifier");

CREATE INDEX IF NOT EXISTS "index_two_factors_on_secret" ON "two_factors" ("secret");

CREATE INDEX IF NOT EXISTS "index_two_factors_on_user_id" ON "two_factors" ("user_id");

CREATE INDEX IF NOT EXISTS "index_members_on_organization_id" ON "members" ("organization_id");

CREATE INDEX IF NOT EXISTS "index_members_on_user_id" ON "members" ("user_id");

CREATE INDEX IF NOT EXISTS "index_invitations_on_organization_id" ON "invitations" ("organization_id");

CREATE INDEX IF NOT EXISTS "index_invitations_on_email" ON "invitations" ("email");

CREATE INDEX IF NOT EXISTS "index_teams_on_organization_id" ON "teams" ("organization_id");

CREATE INDEX IF NOT EXISTS "index_team_members_on_team_id" ON "team_members" ("team_id");

CREATE INDEX IF NOT EXISTS "index_team_members_on_user_id" ON "team_members" ("user_id");

CREATE INDEX IF NOT EXISTS "index_organization_roles_on_organization_id" ON "organization_roles" ("organization_id");

CREATE INDEX IF NOT EXISTS "index_organization_roles_on_role" ON "organization_roles" ("role");

CREATE INDEX IF NOT EXISTS "index_wallet_addresses_on_user_id" ON "wallet_addresses" ("user_id");

CREATE INDEX IF NOT EXISTS "index_api_keys_on_config_id" ON "api_keys" ("config_id");

CREATE INDEX IF NOT EXISTS "index_api_keys_on_key" ON "api_keys" ("key");

CREATE INDEX IF NOT EXISTS "index_api_keys_on_reference_id" ON "api_keys" ("reference_id");

CREATE INDEX IF NOT EXISTS "index_passkeys_on_user_id" ON "passkeys" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_clients_on_user_id" ON "oauth_clients" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_clients_on_reference_id" ON "oauth_clients" ("reference_id");

CREATE INDEX IF NOT EXISTS "index_oauth_refresh_tokens_on_client_id" ON "oauth_refresh_tokens" ("client_id");

CREATE INDEX IF NOT EXISTS "index_oauth_refresh_tokens_on_session_id" ON "oauth_refresh_tokens" ("session_id");

CREATE INDEX IF NOT EXISTS "index_oauth_refresh_tokens_on_user_id" ON "oauth_refresh_tokens" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_refresh_tokens_on_reference_id" ON "oauth_refresh_tokens" ("reference_id");

CREATE INDEX IF NOT EXISTS "index_oauth_access_tokens_on_client_id" ON "oauth_access_tokens" ("client_id");

CREATE INDEX IF NOT EXISTS "index_oauth_access_tokens_on_user_id" ON "oauth_access_tokens" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_access_tokens_on_session_id" ON "oauth_access_tokens" ("session_id");

CREATE INDEX IF NOT EXISTS "index_oauth_access_tokens_on_refresh_id" ON "oauth_access_tokens" ("refresh_id");

CREATE INDEX IF NOT EXISTS "index_oauth_consents_on_client_id" ON "oauth_consents" ("client_id");

CREATE INDEX IF NOT EXISTS "index_oauth_consents_on_user_id" ON "oauth_consents" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_consents_on_reference_id" ON "oauth_consents" ("reference_id");
```

### mysql

```sql
CREATE TABLE IF NOT EXISTS `users` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `name` varchar(191) NOT NULL,
  `email` varchar(191) NOT NULL,
  `email_verified` tinyint(1) NOT NULL DEFAULT 0,
  `image` text,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `username` varchar(191),
  `display_username` text,
  `is_anonymous` tinyint(1) DEFAULT 0,
  `phone_number` varchar(191),
  `phone_number_verified` tinyint(1),
  `two_factor_enabled` tinyint(1) DEFAULT 0,
  `lastLoginMethod` text,
  `stripe_customer_id` text,
  UNIQUE KEY `uniq_users_email` (`email`),
  UNIQUE KEY `uniq_users_username` (`username`),
  UNIQUE KEY `uniq_users_phone_number` (`phone_number`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `sessions` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `expires_at` datetime(6) NOT NULL,
  `token` varchar(191) NOT NULL,
  `ip_address` text,
  `user_agent` text,
  `user_id` varchar(191) NOT NULL,
  `active_organization_id` text,
  `active_team_id` text,
  UNIQUE KEY `uniq_sessions_token` (`token`),
  CONSTRAINT `fk_sessions_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `accounts` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `account_id` text NOT NULL,
  `provider_id` text NOT NULL,
  `user_id` varchar(191) NOT NULL,
  `access_token` text,
  `refresh_token` text,
  `id_token` text,
  `access_token_expires_at` datetime(6),
  `refresh_token_expires_at` datetime(6),
  `scope` text,
  `password` text,
  CONSTRAINT `fk_accounts_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `verifications` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `identifier` varchar(191) NOT NULL,
  `value` text NOT NULL,
  `expires_at` datetime(6) NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `two_factors` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `secret` varchar(191) NOT NULL,
  `backup_codes` text NOT NULL,
  `user_id` varchar(191) NOT NULL,
  `verified` tinyint(1) DEFAULT 1,
  `failed_verification_count` integer DEFAULT 0,
  `locked_until` datetime(6),
  CONSTRAINT `fk_two_factors_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `organizations` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `name` varchar(191) NOT NULL,
  `slug` varchar(191) NOT NULL,
  `logo` text,
  `metadata` text,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6),
  `stripe_customer_id` text,
  UNIQUE KEY `uniq_organizations_slug` (`slug`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `members` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `organization_id` varchar(191) NOT NULL,
  `user_id` varchar(191) NOT NULL,
  `role` varchar(191) NOT NULL DEFAULT 'member',
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  CONSTRAINT `fk_members_organization_id` FOREIGN KEY (`organization_id`) REFERENCES `organizations` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_members_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `invitations` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `organization_id` varchar(191) NOT NULL,
  `email` varchar(191) NOT NULL,
  `role` varchar(191) NOT NULL,
  `status` varchar(191) NOT NULL DEFAULT 'pending',
  `expires_at` datetime(6) NOT NULL,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `inviter_id` varchar(191) NOT NULL,
  `team_id` varchar(191),
  CONSTRAINT `fk_invitations_organization_id` FOREIGN KEY (`organization_id`) REFERENCES `organizations` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_invitations_inviter_id` FOREIGN KEY (`inviter_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `teams` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `name` text NOT NULL,
  `organization_id` varchar(191) NOT NULL,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6),
  CONSTRAINT `fk_teams_organization_id` FOREIGN KEY (`organization_id`) REFERENCES `organizations` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `team_members` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `team_id` varchar(191) NOT NULL,
  `user_id` varchar(191) NOT NULL,
  `created_at` datetime(6) DEFAULT CURRENT_TIMESTAMP(6),
  CONSTRAINT `fk_team_members_team_id` FOREIGN KEY (`team_id`) REFERENCES `teams` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_team_members_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `organization_roles` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `organization_id` varchar(191) NOT NULL,
  `role` varchar(191) NOT NULL,
  `permission` text NOT NULL,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6),
  CONSTRAINT `fk_organization_roles_organization_id` FOREIGN KEY (`organization_id`) REFERENCES `organizations` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `jwks` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `public_key` text NOT NULL,
  `private_key` text NOT NULL,
  `created_at` datetime(6) NOT NULL,
  `expires_at` datetime(6),
  `alg` text,
  `kty` text,
  `crv` text,
  `x` text,
  `y` text,
  `pem` text,
  `n` text,
  `e` text
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `device_codes` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `device_code` text NOT NULL,
  `user_code` text NOT NULL,
  `user_id` text,
  `expires_at` datetime(6) NOT NULL,
  `status` text NOT NULL,
  `last_polled_at` datetime(6),
  `polling_interval` integer,
  `client_id` text,
  `scope` text
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `wallet_addresses` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `user_id` varchar(191) NOT NULL,
  `address` text NOT NULL,
  `chain_id` integer NOT NULL,
  `is_primary` tinyint(1) DEFAULT 0,
  `created_at` datetime(6) NOT NULL,
  CONSTRAINT `fk_wallet_addresses_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `api_keys` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `config_id` varchar(191) NOT NULL DEFAULT 'default',
  `name` text,
  `start` text,
  `prefix` text,
  `key` varchar(191) NOT NULL,
  `reference_id` varchar(191) NOT NULL,
  `refill_interval` integer,
  `refill_amount` integer,
  `last_refill_at` datetime(6),
  `enabled` tinyint(1) DEFAULT 1,
  `rate_limit_enabled` tinyint(1) DEFAULT 1,
  `rate_limit_time_window` integer DEFAULT 86400000,
  `rate_limit_max` integer DEFAULT 10,
  `request_count` integer DEFAULT 0,
  `remaining` integer,
  `last_request` datetime(6),
  `expires_at` datetime(6),
  `created_at` datetime(6) NOT NULL,
  `updated_at` datetime(6) NOT NULL,
  `permissions` text,
  `metadata` text
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `passkeys` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `name` text,
  `public_key` text NOT NULL,
  `user_id` varchar(191) NOT NULL,
  `credential_id` varchar(191) NOT NULL,
  `counter` integer NOT NULL,
  `device_type` text NOT NULL,
  `backed_up` tinyint(1) NOT NULL,
  `transports` text,
  `created_at` datetime(6),
  `aaguid` text,
  CONSTRAINT `fk_passkeys_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  UNIQUE KEY `uniq_passkeys_credential_id` (`credential_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `oauth_clients` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `client_id` varchar(191) NOT NULL,
  `client_secret` text,
  `disabled` tinyint(1) DEFAULT 0,
  `skip_consent` tinyint(1),
  `enable_end_session` tinyint(1),
  `client_secret_expires_at` integer,
  `scopes` json,
  `user_id` varchar(191),
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `name` text,
  `uri` text,
  `icon` text,
  `contacts` json,
  `tos` text,
  `policy` text,
  `software_id` text,
  `software_version` text,
  `software_statement` text,
  `redirect_uris` json NOT NULL,
  `post_logout_redirect_uris` json,
  `token_endpoint_auth_method` text,
  `grant_types` json,
  `response_types` json,
  `public` tinyint(1),
  `type` text,
  `require_pkce` tinyint(1),
  `subject_type` text,
  `reference_id` varchar(191),
  `metadata` json,
  UNIQUE KEY `uniq_oauth_clients_client_id` (`client_id`),
  CONSTRAINT `fk_oauth_clients_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `oauth_refresh_tokens` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `token` text NOT NULL,
  `client_id` varchar(191) NOT NULL,
  `session_id` varchar(191),
  `user_id` varchar(191),
  `reference_id` varchar(191),
  `auth_time` datetime(6),
  `expires_at` datetime(6),
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `revoked` datetime(6),
  `scopes` json NOT NULL,
  CONSTRAINT `fk_oauth_refresh_tokens_client_id` FOREIGN KEY (`client_id`) REFERENCES `oauth_clients` (`client_id`) ON DELETE CASCADE,
  CONSTRAINT `fk_oauth_refresh_tokens_session_id` FOREIGN KEY (`session_id`) REFERENCES `sessions` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_oauth_refresh_tokens_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `oauth_access_tokens` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `token` varchar(191) NOT NULL,
  `expires_at` datetime(6) NOT NULL,
  `client_id` varchar(191) NOT NULL,
  `user_id` varchar(191),
  `session_id` varchar(191),
  `scopes` json NOT NULL,
  `revoked` datetime(6),
  `reference_id` text,
  `auth_time` datetime(6),
  `refresh_id` varchar(191),
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  UNIQUE KEY `uniq_oauth_access_tokens_token` (`token`),
  CONSTRAINT `fk_oauth_access_tokens_client_id` FOREIGN KEY (`client_id`) REFERENCES `oauth_clients` (`client_id`) ON DELETE CASCADE,
  CONSTRAINT `fk_oauth_access_tokens_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_oauth_access_tokens_session_id` FOREIGN KEY (`session_id`) REFERENCES `sessions` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_oauth_access_tokens_refresh_id` FOREIGN KEY (`refresh_id`) REFERENCES `oauth_refresh_tokens` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `oauth_consents` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `client_id` varchar(191) NOT NULL,
  `user_id` varchar(191),
  `reference_id` varchar(191),
  `scopes` json NOT NULL,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  `updated_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  CONSTRAINT `fk_oauth_consents_client_id` FOREIGN KEY (`client_id`) REFERENCES `oauth_clients` (`client_id`) ON DELETE CASCADE,
  CONSTRAINT `fk_oauth_consents_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `scim_providers` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `provider_id` varchar(191) NOT NULL,
  `scim_token` varchar(191) NOT NULL,
  `organization_id` text,
  `user_id` text,
  UNIQUE KEY `uniq_scim_providers_provider_id` (`provider_id`),
  UNIQUE KEY `uniq_scim_providers_scim_token` (`scim_token`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `sso_providers` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `issuer` text NOT NULL,
  `oidc_config` text,
  `saml_config` text,
  `user_id` varchar(191) NOT NULL,
  `provider_id` varchar(191) NOT NULL,
  `domain` text NOT NULL,
  `organization_id` text,
  `domain_verified` tinyint(1) DEFAULT 0,
  CONSTRAINT `fk_sso_providers_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  UNIQUE KEY `uniq_sso_providers_provider_id` (`provider_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `subscriptions` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `plan` text NOT NULL,
  `reference_id` text NOT NULL,
  `stripe_customer_id` text,
  `stripe_subscription_id` text,
  `status` varchar(191) DEFAULT 'incomplete',
  `period_start` datetime(6),
  `period_end` datetime(6),
  `trial_start` datetime(6),
  `trial_end` datetime(6),
  `cancel_at_period_end` tinyint(1) DEFAULT 0,
  `cancel_at` datetime(6),
  `canceled_at` datetime(6),
  `ended_at` datetime(6),
  `seats` integer,
  `billing_interval` text,
  `stripe_schedule_id` text,
  `limits` json
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `rate_limits` (
  `id` varchar(191) PRIMARY KEY NOT NULL,
  `key` varchar(191) NOT NULL,
  `count` integer NOT NULL,
  `last_request` bigint NOT NULL,
  UNIQUE KEY `uniq_rate_limits_key` (`key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE INDEX `index_sessions_on_user_id` ON `sessions` (`user_id`);

CREATE INDEX `index_accounts_on_user_id` ON `accounts` (`user_id`);

CREATE INDEX `index_verifications_on_identifier` ON `verifications` (`identifier`);

CREATE INDEX `index_two_factors_on_secret` ON `two_factors` (`secret`);

CREATE INDEX `index_two_factors_on_user_id` ON `two_factors` (`user_id`);

CREATE INDEX `index_members_on_organization_id` ON `members` (`organization_id`);

CREATE INDEX `index_members_on_user_id` ON `members` (`user_id`);

CREATE INDEX `index_invitations_on_organization_id` ON `invitations` (`organization_id`);

CREATE INDEX `index_invitations_on_email` ON `invitations` (`email`);

CREATE INDEX `index_teams_on_organization_id` ON `teams` (`organization_id`);

CREATE INDEX `index_team_members_on_team_id` ON `team_members` (`team_id`);

CREATE INDEX `index_team_members_on_user_id` ON `team_members` (`user_id`);

CREATE INDEX `index_organization_roles_on_organization_id` ON `organization_roles` (`organization_id`);

CREATE INDEX `index_organization_roles_on_role` ON `organization_roles` (`role`);

CREATE INDEX `index_wallet_addresses_on_user_id` ON `wallet_addresses` (`user_id`);

CREATE INDEX `index_api_keys_on_config_id` ON `api_keys` (`config_id`);

CREATE INDEX `index_api_keys_on_key` ON `api_keys` (`key`);

CREATE INDEX `index_api_keys_on_reference_id` ON `api_keys` (`reference_id`);

CREATE INDEX `index_passkeys_on_user_id` ON `passkeys` (`user_id`);

CREATE INDEX `index_oauth_clients_on_user_id` ON `oauth_clients` (`user_id`);

CREATE INDEX `index_oauth_clients_on_reference_id` ON `oauth_clients` (`reference_id`);

CREATE INDEX `index_oauth_refresh_tokens_on_client_id` ON `oauth_refresh_tokens` (`client_id`);

CREATE INDEX `index_oauth_refresh_tokens_on_session_id` ON `oauth_refresh_tokens` (`session_id`);

CREATE INDEX `index_oauth_refresh_tokens_on_user_id` ON `oauth_refresh_tokens` (`user_id`);

CREATE INDEX `index_oauth_refresh_tokens_on_reference_id` ON `oauth_refresh_tokens` (`reference_id`);

CREATE INDEX `index_oauth_access_tokens_on_client_id` ON `oauth_access_tokens` (`client_id`);

CREATE INDEX `index_oauth_access_tokens_on_user_id` ON `oauth_access_tokens` (`user_id`);

CREATE INDEX `index_oauth_access_tokens_on_session_id` ON `oauth_access_tokens` (`session_id`);

CREATE INDEX `index_oauth_access_tokens_on_refresh_id` ON `oauth_access_tokens` (`refresh_id`);

CREATE INDEX `index_oauth_consents_on_client_id` ON `oauth_consents` (`client_id`);

CREATE INDEX `index_oauth_consents_on_user_id` ON `oauth_consents` (`user_id`);

CREATE INDEX `index_oauth_consents_on_reference_id` ON `oauth_consents` (`reference_id`);
```

### sqlite

```sql
CREATE TABLE IF NOT EXISTS "users" (
  "id" text PRIMARY KEY NOT NULL,
  "name" text NOT NULL,
  "email" text NOT NULL,
  "email_verified" integer NOT NULL DEFAULT 0,
  "image" text,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "username" text,
  "display_username" text,
  "is_anonymous" integer DEFAULT 0,
  "phone_number" text,
  "phone_number_verified" integer,
  "two_factor_enabled" integer DEFAULT 0,
  "lastLoginMethod" text,
  "stripe_customer_id" text,
  UNIQUE ("email"),
  UNIQUE ("username"),
  UNIQUE ("phone_number")
);

CREATE TABLE IF NOT EXISTS "sessions" (
  "id" text PRIMARY KEY NOT NULL,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "expires_at" date NOT NULL,
  "token" text NOT NULL,
  "ip_address" text,
  "user_agent" text,
  "user_id" text NOT NULL,
  "active_organization_id" text,
  "active_team_id" text,
  UNIQUE ("token"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "accounts" (
  "id" text PRIMARY KEY NOT NULL,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "account_id" text NOT NULL,
  "provider_id" text NOT NULL,
  "user_id" text NOT NULL,
  "access_token" text,
  "refresh_token" text,
  "id_token" text,
  "access_token_expires_at" date,
  "refresh_token_expires_at" date,
  "scope" text,
  "password" text,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "verifications" (
  "id" text PRIMARY KEY NOT NULL,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "identifier" text NOT NULL,
  "value" text NOT NULL,
  "expires_at" date NOT NULL
);

CREATE TABLE IF NOT EXISTS "two_factors" (
  "id" text PRIMARY KEY NOT NULL,
  "secret" text NOT NULL,
  "backup_codes" text NOT NULL,
  "user_id" text NOT NULL,
  "verified" integer DEFAULT 1,
  "failed_verification_count" integer DEFAULT 0,
  "locked_until" date,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "organizations" (
  "id" text PRIMARY KEY NOT NULL,
  "name" text NOT NULL,
  "slug" text NOT NULL,
  "logo" text,
  "metadata" text,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date,
  "stripe_customer_id" text,
  UNIQUE ("slug")
);

CREATE TABLE IF NOT EXISTS "members" (
  "id" text PRIMARY KEY NOT NULL,
  "organization_id" text NOT NULL,
  "user_id" text NOT NULL,
  "role" text NOT NULL DEFAULT 'member',
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY ("organization_id") REFERENCES "organizations" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "invitations" (
  "id" text PRIMARY KEY NOT NULL,
  "organization_id" text NOT NULL,
  "email" text NOT NULL,
  "role" text NOT NULL,
  "status" text NOT NULL DEFAULT 'pending',
  "expires_at" date NOT NULL,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "inviter_id" text NOT NULL,
  "team_id" text,
  FOREIGN KEY ("organization_id") REFERENCES "organizations" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("inviter_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "teams" (
  "id" text PRIMARY KEY NOT NULL,
  "name" text NOT NULL,
  "organization_id" text NOT NULL,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date,
  FOREIGN KEY ("organization_id") REFERENCES "organizations" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "team_members" (
  "id" text PRIMARY KEY NOT NULL,
  "team_id" text NOT NULL,
  "user_id" text NOT NULL,
  "created_at" date DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY ("team_id") REFERENCES "teams" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "organization_roles" (
  "id" text PRIMARY KEY NOT NULL,
  "organization_id" text NOT NULL,
  "role" text NOT NULL,
  "permission" text NOT NULL,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date,
  FOREIGN KEY ("organization_id") REFERENCES "organizations" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "jwks" (
  "id" text PRIMARY KEY NOT NULL,
  "public_key" text NOT NULL,
  "private_key" text NOT NULL,
  "created_at" date NOT NULL,
  "expires_at" date,
  "alg" text,
  "kty" text,
  "crv" text,
  "x" text,
  "y" text,
  "pem" text,
  "n" text,
  "e" text
);

CREATE TABLE IF NOT EXISTS "device_codes" (
  "id" text PRIMARY KEY NOT NULL,
  "device_code" text NOT NULL,
  "user_code" text NOT NULL,
  "user_id" text,
  "expires_at" date NOT NULL,
  "status" text NOT NULL,
  "last_polled_at" date,
  "polling_interval" integer,
  "client_id" text,
  "scope" text
);

CREATE TABLE IF NOT EXISTS "wallet_addresses" (
  "id" text PRIMARY KEY NOT NULL,
  "user_id" text NOT NULL,
  "address" text NOT NULL,
  "chain_id" integer NOT NULL,
  "is_primary" integer DEFAULT 0,
  "created_at" date NOT NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "api_keys" (
  "id" text PRIMARY KEY NOT NULL,
  "config_id" text NOT NULL DEFAULT 'default',
  "name" text,
  "start" text,
  "prefix" text,
  "key" text NOT NULL,
  "reference_id" text NOT NULL,
  "refill_interval" integer,
  "refill_amount" integer,
  "last_refill_at" date,
  "enabled" integer DEFAULT 1,
  "rate_limit_enabled" integer DEFAULT 1,
  "rate_limit_time_window" integer DEFAULT 86400000,
  "rate_limit_max" integer DEFAULT 10,
  "request_count" integer DEFAULT 0,
  "remaining" integer,
  "last_request" date,
  "expires_at" date,
  "created_at" date NOT NULL,
  "updated_at" date NOT NULL,
  "permissions" text,
  "metadata" text
);

CREATE TABLE IF NOT EXISTS "passkeys" (
  "id" text PRIMARY KEY NOT NULL,
  "name" text,
  "public_key" text NOT NULL,
  "user_id" text NOT NULL,
  "credential_id" text NOT NULL,
  "counter" integer NOT NULL,
  "device_type" text NOT NULL,
  "backed_up" integer NOT NULL,
  "transports" text,
  "created_at" date,
  "aaguid" text,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE,
  UNIQUE ("credential_id")
);

CREATE TABLE IF NOT EXISTS "oauth_clients" (
  "id" text PRIMARY KEY NOT NULL,
  "client_id" text NOT NULL,
  "client_secret" text,
  "disabled" integer DEFAULT 0,
  "skip_consent" integer,
  "enable_end_session" integer,
  "client_secret_expires_at" integer,
  "scopes" text,
  "user_id" text,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "name" text,
  "uri" text,
  "icon" text,
  "contacts" text,
  "tos" text,
  "policy" text,
  "software_id" text,
  "software_version" text,
  "software_statement" text,
  "redirect_uris" text NOT NULL,
  "post_logout_redirect_uris" text,
  "token_endpoint_auth_method" text,
  "grant_types" text,
  "response_types" text,
  "public" integer,
  "type" text,
  "require_pkce" integer,
  "subject_type" text,
  "reference_id" text,
  "metadata" text,
  UNIQUE ("client_id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "oauth_refresh_tokens" (
  "id" text PRIMARY KEY NOT NULL,
  "token" text NOT NULL,
  "client_id" text NOT NULL,
  "session_id" text,
  "user_id" text,
  "reference_id" text,
  "auth_time" date,
  "expires_at" date,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "revoked" date,
  "scopes" text NOT NULL,
  FOREIGN KEY ("client_id") REFERENCES "oauth_clients" ("client_id") ON DELETE CASCADE,
  FOREIGN KEY ("session_id") REFERENCES "sessions" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "oauth_access_tokens" (
  "id" text PRIMARY KEY NOT NULL,
  "token" text NOT NULL,
  "expires_at" date NOT NULL,
  "client_id" text NOT NULL,
  "user_id" text,
  "session_id" text,
  "scopes" text NOT NULL,
  "revoked" date,
  "reference_id" text,
  "auth_time" date,
  "refresh_id" text,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE ("token"),
  FOREIGN KEY ("client_id") REFERENCES "oauth_clients" ("client_id") ON DELETE CASCADE,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("session_id") REFERENCES "sessions" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("refresh_id") REFERENCES "oauth_refresh_tokens" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "oauth_consents" (
  "id" text PRIMARY KEY NOT NULL,
  "client_id" text NOT NULL,
  "user_id" text,
  "reference_id" text,
  "scopes" text NOT NULL,
  "created_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" date NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY ("client_id") REFERENCES "oauth_clients" ("client_id") ON DELETE CASCADE,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "scim_providers" (
  "id" text PRIMARY KEY NOT NULL,
  "provider_id" text NOT NULL,
  "scim_token" text NOT NULL,
  "organization_id" text,
  "user_id" text,
  UNIQUE ("provider_id"),
  UNIQUE ("scim_token")
);

CREATE TABLE IF NOT EXISTS "sso_providers" (
  "id" text PRIMARY KEY NOT NULL,
  "issuer" text NOT NULL,
  "oidc_config" text,
  "saml_config" text,
  "user_id" text NOT NULL,
  "provider_id" text NOT NULL,
  "domain" text NOT NULL,
  "organization_id" text,
  "domain_verified" integer DEFAULT 0,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE,
  UNIQUE ("provider_id")
);

CREATE TABLE IF NOT EXISTS "subscriptions" (
  "id" text PRIMARY KEY NOT NULL,
  "plan" text NOT NULL,
  "reference_id" text NOT NULL,
  "stripe_customer_id" text,
  "stripe_subscription_id" text,
  "status" text DEFAULT 'incomplete',
  "period_start" date,
  "period_end" date,
  "trial_start" date,
  "trial_end" date,
  "cancel_at_period_end" integer DEFAULT 0,
  "cancel_at" date,
  "canceled_at" date,
  "ended_at" date,
  "seats" integer,
  "billing_interval" text,
  "stripe_schedule_id" text,
  "limits" text
);

CREATE TABLE IF NOT EXISTS "rate_limits" (
  "id" text PRIMARY KEY NOT NULL,
  "key" text NOT NULL,
  "count" integer NOT NULL,
  "last_request" bigint NOT NULL,
  UNIQUE ("key")
);

CREATE INDEX IF NOT EXISTS "index_sessions_on_user_id" ON "sessions" ("user_id");

CREATE INDEX IF NOT EXISTS "index_accounts_on_user_id" ON "accounts" ("user_id");

CREATE INDEX IF NOT EXISTS "index_verifications_on_identifier" ON "verifications" ("identifier");

CREATE INDEX IF NOT EXISTS "index_two_factors_on_secret" ON "two_factors" ("secret");

CREATE INDEX IF NOT EXISTS "index_two_factors_on_user_id" ON "two_factors" ("user_id");

CREATE INDEX IF NOT EXISTS "index_members_on_organization_id" ON "members" ("organization_id");

CREATE INDEX IF NOT EXISTS "index_members_on_user_id" ON "members" ("user_id");

CREATE INDEX IF NOT EXISTS "index_invitations_on_organization_id" ON "invitations" ("organization_id");

CREATE INDEX IF NOT EXISTS "index_invitations_on_email" ON "invitations" ("email");

CREATE INDEX IF NOT EXISTS "index_teams_on_organization_id" ON "teams" ("organization_id");

CREATE INDEX IF NOT EXISTS "index_team_members_on_team_id" ON "team_members" ("team_id");

CREATE INDEX IF NOT EXISTS "index_team_members_on_user_id" ON "team_members" ("user_id");

CREATE INDEX IF NOT EXISTS "index_organization_roles_on_organization_id" ON "organization_roles" ("organization_id");

CREATE INDEX IF NOT EXISTS "index_organization_roles_on_role" ON "organization_roles" ("role");

CREATE INDEX IF NOT EXISTS "index_wallet_addresses_on_user_id" ON "wallet_addresses" ("user_id");

CREATE INDEX IF NOT EXISTS "index_api_keys_on_config_id" ON "api_keys" ("config_id");

CREATE INDEX IF NOT EXISTS "index_api_keys_on_key" ON "api_keys" ("key");

CREATE INDEX IF NOT EXISTS "index_api_keys_on_reference_id" ON "api_keys" ("reference_id");

CREATE INDEX IF NOT EXISTS "index_passkeys_on_user_id" ON "passkeys" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_clients_on_user_id" ON "oauth_clients" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_clients_on_reference_id" ON "oauth_clients" ("reference_id");

CREATE INDEX IF NOT EXISTS "index_oauth_refresh_tokens_on_client_id" ON "oauth_refresh_tokens" ("client_id");

CREATE INDEX IF NOT EXISTS "index_oauth_refresh_tokens_on_session_id" ON "oauth_refresh_tokens" ("session_id");

CREATE INDEX IF NOT EXISTS "index_oauth_refresh_tokens_on_user_id" ON "oauth_refresh_tokens" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_refresh_tokens_on_reference_id" ON "oauth_refresh_tokens" ("reference_id");

CREATE INDEX IF NOT EXISTS "index_oauth_access_tokens_on_client_id" ON "oauth_access_tokens" ("client_id");

CREATE INDEX IF NOT EXISTS "index_oauth_access_tokens_on_user_id" ON "oauth_access_tokens" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_access_tokens_on_session_id" ON "oauth_access_tokens" ("session_id");

CREATE INDEX IF NOT EXISTS "index_oauth_access_tokens_on_refresh_id" ON "oauth_access_tokens" ("refresh_id");

CREATE INDEX IF NOT EXISTS "index_oauth_consents_on_client_id" ON "oauth_consents" ("client_id");

CREATE INDEX IF NOT EXISTS "index_oauth_consents_on_user_id" ON "oauth_consents" ("user_id");

CREATE INDEX IF NOT EXISTS "index_oauth_consents_on_reference_id" ON "oauth_consents" ("reference_id");
```

### mssql

```sql
SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[users]', N'U') IS NULL
CREATE TABLE [users] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [name] varchar(255) NOT NULL,
  [email] varchar(255) NOT NULL,
  [email_verified] smallint NOT NULL DEFAULT 0,
  [image] varchar(8000) NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [username] varchar(255) NULL,
  [display_username] varchar(8000) NULL,
  [is_anonymous] smallint NULL DEFAULT 0,
  [phone_number] varchar(255) NULL,
  [phone_number_verified] smallint NULL,
  [two_factor_enabled] smallint NULL DEFAULT 0,
  [lastLoginMethod] varchar(8000) NULL,
  [stripe_customer_id] varchar(8000) NULL,
  CONSTRAINT [uniq_users_email] UNIQUE ([email])
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[sessions]', N'U') IS NULL
CREATE TABLE [sessions] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [expires_at] datetime2(3) NOT NULL,
  [token] varchar(255) NOT NULL,
  [ip_address] varchar(8000) NULL,
  [user_agent] varchar(8000) NULL,
  [user_id] varchar(255) NOT NULL,
  [active_organization_id] varchar(8000) NULL,
  [active_team_id] varchar(8000) NULL,
  CONSTRAINT [uniq_sessions_token] UNIQUE ([token]),
  CONSTRAINT [fk_sessions_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[accounts]', N'U') IS NULL
CREATE TABLE [accounts] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [account_id] varchar(8000) NOT NULL,
  [provider_id] varchar(8000) NOT NULL,
  [user_id] varchar(255) NOT NULL,
  [access_token] varchar(8000) NULL,
  [refresh_token] varchar(8000) NULL,
  [id_token] varchar(8000) NULL,
  [access_token_expires_at] datetime2(3) NULL,
  [refresh_token_expires_at] datetime2(3) NULL,
  [scope] varchar(8000) NULL,
  [password] varchar(8000) NULL,
  CONSTRAINT [fk_accounts_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[verifications]', N'U') IS NULL
CREATE TABLE [verifications] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [identifier] varchar(255) NOT NULL,
  [value] varchar(8000) NOT NULL,
  [expires_at] datetime2(3) NOT NULL
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[two_factors]', N'U') IS NULL
CREATE TABLE [two_factors] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [secret] varchar(255) NOT NULL,
  [backup_codes] varchar(8000) NOT NULL,
  [user_id] varchar(255) NOT NULL,
  [verified] smallint NULL DEFAULT 1,
  [failed_verification_count] integer NULL DEFAULT 0,
  [locked_until] datetime2(3) NULL,
  CONSTRAINT [fk_two_factors_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[organizations]', N'U') IS NULL
CREATE TABLE [organizations] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [name] varchar(255) NOT NULL,
  [slug] varchar(255) NOT NULL,
  [logo] varchar(8000) NULL,
  [metadata] varchar(8000) NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NULL,
  [stripe_customer_id] varchar(8000) NULL,
  CONSTRAINT [uniq_organizations_slug] UNIQUE ([slug])
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[members]', N'U') IS NULL
CREATE TABLE [members] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [organization_id] varchar(255) NOT NULL,
  [user_id] varchar(255) NOT NULL,
  [role] varchar(255) NOT NULL DEFAULT 'member',
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT [fk_members_organization_id] FOREIGN KEY ([organization_id]) REFERENCES [organizations] ([id]) ON DELETE CASCADE,
  CONSTRAINT [fk_members_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[invitations]', N'U') IS NULL
CREATE TABLE [invitations] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [organization_id] varchar(255) NOT NULL,
  [email] varchar(255) NOT NULL,
  [role] varchar(255) NOT NULL,
  [status] varchar(255) NOT NULL DEFAULT 'pending',
  [expires_at] datetime2(3) NOT NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [inviter_id] varchar(255) NOT NULL,
  [team_id] varchar(255) NULL,
  CONSTRAINT [fk_invitations_organization_id] FOREIGN KEY ([organization_id]) REFERENCES [organizations] ([id]) ON DELETE CASCADE,
  CONSTRAINT [fk_invitations_inviter_id] FOREIGN KEY ([inviter_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[teams]', N'U') IS NULL
CREATE TABLE [teams] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [name] varchar(8000) NOT NULL,
  [organization_id] varchar(255) NOT NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NULL,
  CONSTRAINT [fk_teams_organization_id] FOREIGN KEY ([organization_id]) REFERENCES [organizations] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[team_members]', N'U') IS NULL
CREATE TABLE [team_members] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [team_id] varchar(255) NOT NULL,
  [user_id] varchar(255) NOT NULL,
  [created_at] datetime2(3) NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT [fk_team_members_team_id] FOREIGN KEY ([team_id]) REFERENCES [teams] ([id]) ON DELETE CASCADE,
  CONSTRAINT [fk_team_members_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[organization_roles]', N'U') IS NULL
CREATE TABLE [organization_roles] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [organization_id] varchar(255) NOT NULL,
  [role] varchar(255) NOT NULL,
  [permission] varchar(8000) NOT NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NULL,
  CONSTRAINT [fk_organization_roles_organization_id] FOREIGN KEY ([organization_id]) REFERENCES [organizations] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[jwks]', N'U') IS NULL
CREATE TABLE [jwks] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [public_key] varchar(8000) NOT NULL,
  [private_key] varchar(8000) NOT NULL,
  [created_at] datetime2(3) NOT NULL,
  [expires_at] datetime2(3) NULL,
  [alg] varchar(8000) NULL,
  [kty] varchar(8000) NULL,
  [crv] varchar(8000) NULL,
  [x] varchar(8000) NULL,
  [y] varchar(8000) NULL,
  [pem] varchar(8000) NULL,
  [n] varchar(8000) NULL,
  [e] varchar(8000) NULL
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[device_codes]', N'U') IS NULL
CREATE TABLE [device_codes] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [device_code] varchar(8000) NOT NULL,
  [user_code] varchar(8000) NOT NULL,
  [user_id] varchar(8000) NULL,
  [expires_at] datetime2(3) NOT NULL,
  [status] varchar(8000) NOT NULL,
  [last_polled_at] datetime2(3) NULL,
  [polling_interval] integer NULL,
  [client_id] varchar(8000) NULL,
  [scope] varchar(8000) NULL
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[wallet_addresses]', N'U') IS NULL
CREATE TABLE [wallet_addresses] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [user_id] varchar(255) NOT NULL,
  [address] varchar(8000) NOT NULL,
  [chain_id] integer NOT NULL,
  [is_primary] smallint NULL DEFAULT 0,
  [created_at] datetime2(3) NOT NULL,
  CONSTRAINT [fk_wallet_addresses_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[api_keys]', N'U') IS NULL
CREATE TABLE [api_keys] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [config_id] varchar(255) NOT NULL DEFAULT 'default',
  [name] varchar(8000) NULL,
  [start] varchar(8000) NULL,
  [prefix] varchar(8000) NULL,
  [key] varchar(255) NOT NULL,
  [reference_id] varchar(255) NOT NULL,
  [refill_interval] integer NULL,
  [refill_amount] integer NULL,
  [last_refill_at] datetime2(3) NULL,
  [enabled] smallint NULL DEFAULT 1,
  [rate_limit_enabled] smallint NULL DEFAULT 1,
  [rate_limit_time_window] integer NULL DEFAULT 86400000,
  [rate_limit_max] integer NULL DEFAULT 10,
  [request_count] integer NULL DEFAULT 0,
  [remaining] integer NULL,
  [last_request] datetime2(3) NULL,
  [expires_at] datetime2(3) NULL,
  [created_at] datetime2(3) NOT NULL,
  [updated_at] datetime2(3) NOT NULL,
  [permissions] varchar(8000) NULL,
  [metadata] varchar(8000) NULL
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[passkeys]', N'U') IS NULL
CREATE TABLE [passkeys] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [name] varchar(8000) NULL,
  [public_key] varchar(8000) NOT NULL,
  [user_id] varchar(255) NOT NULL,
  [credential_id] varchar(255) NOT NULL,
  [counter] integer NOT NULL,
  [device_type] varchar(8000) NOT NULL,
  [backed_up] smallint NOT NULL,
  [transports] varchar(8000) NULL,
  [created_at] datetime2(3) NULL,
  [aaguid] varchar(8000) NULL,
  CONSTRAINT [fk_passkeys_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE,
  CONSTRAINT [uniq_passkeys_credential_id] UNIQUE ([credential_id])
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[oauth_clients]', N'U') IS NULL
CREATE TABLE [oauth_clients] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [client_id] varchar(255) NOT NULL,
  [client_secret] varchar(8000) NULL,
  [disabled] smallint NULL DEFAULT 0,
  [skip_consent] smallint NULL,
  [enable_end_session] smallint NULL,
  [client_secret_expires_at] integer NULL,
  [scopes] varchar(8000) NULL,
  [user_id] varchar(255) NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [name] varchar(8000) NULL,
  [uri] varchar(8000) NULL,
  [icon] varchar(8000) NULL,
  [contacts] varchar(8000) NULL,
  [tos] varchar(8000) NULL,
  [policy] varchar(8000) NULL,
  [software_id] varchar(8000) NULL,
  [software_version] varchar(8000) NULL,
  [software_statement] varchar(8000) NULL,
  [redirect_uris] varchar(8000) NOT NULL,
  [post_logout_redirect_uris] varchar(8000) NULL,
  [token_endpoint_auth_method] varchar(8000) NULL,
  [grant_types] varchar(8000) NULL,
  [response_types] varchar(8000) NULL,
  [public] smallint NULL,
  [type] varchar(8000) NULL,
  [require_pkce] smallint NULL,
  [subject_type] varchar(8000) NULL,
  [reference_id] varchar(255) NULL,
  [metadata] varchar(8000) NULL,
  CONSTRAINT [uniq_oauth_clients_client_id] UNIQUE ([client_id]),
  CONSTRAINT [fk_oauth_clients_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[oauth_refresh_tokens]', N'U') IS NULL
CREATE TABLE [oauth_refresh_tokens] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [token] varchar(8000) NOT NULL,
  [client_id] varchar(255) NOT NULL,
  [session_id] varchar(255) NULL,
  [user_id] varchar(255) NULL,
  [reference_id] varchar(255) NULL,
  [auth_time] datetime2(3) NULL,
  [expires_at] datetime2(3) NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [revoked] datetime2(3) NULL,
  [scopes] varchar(8000) NOT NULL,
  CONSTRAINT [fk_oauth_refresh_tokens_client_id] FOREIGN KEY ([client_id]) REFERENCES [oauth_clients] ([client_id]) ON DELETE CASCADE,
  CONSTRAINT [fk_oauth_refresh_tokens_session_id] FOREIGN KEY ([session_id]) REFERENCES [sessions] ([id]) ON DELETE SET NULL,
  CONSTRAINT [fk_oauth_refresh_tokens_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[oauth_access_tokens]', N'U') IS NULL
CREATE TABLE [oauth_access_tokens] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [token] varchar(255) NOT NULL,
  [expires_at] datetime2(3) NOT NULL,
  [client_id] varchar(255) NOT NULL,
  [user_id] varchar(255) NULL,
  [session_id] varchar(255) NULL,
  [scopes] varchar(8000) NOT NULL,
  [revoked] datetime2(3) NULL,
  [reference_id] varchar(8000) NULL,
  [auth_time] datetime2(3) NULL,
  [refresh_id] varchar(255) NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT [uniq_oauth_access_tokens_token] UNIQUE ([token]),
  CONSTRAINT [fk_oauth_access_tokens_client_id] FOREIGN KEY ([client_id]) REFERENCES [oauth_clients] ([client_id]) ON DELETE CASCADE,
  CONSTRAINT [fk_oauth_access_tokens_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE,
  CONSTRAINT [fk_oauth_access_tokens_session_id] FOREIGN KEY ([session_id]) REFERENCES [sessions] ([id]) ON DELETE SET NULL,
  CONSTRAINT [fk_oauth_access_tokens_refresh_id] FOREIGN KEY ([refresh_id]) REFERENCES [oauth_refresh_tokens] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[oauth_consents]', N'U') IS NULL
CREATE TABLE [oauth_consents] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [client_id] varchar(255) NOT NULL,
  [user_id] varchar(255) NULL,
  [reference_id] varchar(255) NULL,
  [scopes] varchar(8000) NOT NULL,
  [created_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  [updated_at] datetime2(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT [fk_oauth_consents_client_id] FOREIGN KEY ([client_id]) REFERENCES [oauth_clients] ([client_id]) ON DELETE CASCADE,
  CONSTRAINT [fk_oauth_consents_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[scim_providers]', N'U') IS NULL
CREATE TABLE [scim_providers] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [provider_id] varchar(255) NOT NULL,
  [scim_token] varchar(255) NOT NULL,
  [organization_id] varchar(8000) NULL,
  [user_id] varchar(8000) NULL,
  CONSTRAINT [uniq_scim_providers_provider_id] UNIQUE ([provider_id]),
  CONSTRAINT [uniq_scim_providers_scim_token] UNIQUE ([scim_token])
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[sso_providers]', N'U') IS NULL
CREATE TABLE [sso_providers] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [issuer] varchar(8000) NOT NULL,
  [oidc_config] varchar(8000) NULL,
  [saml_config] varchar(8000) NULL,
  [user_id] varchar(255) NOT NULL,
  [provider_id] varchar(255) NOT NULL,
  [domain] varchar(8000) NOT NULL,
  [organization_id] varchar(8000) NULL,
  [domain_verified] smallint NULL DEFAULT 0,
  CONSTRAINT [fk_sso_providers_user_id] FOREIGN KEY ([user_id]) REFERENCES [users] ([id]) ON DELETE CASCADE,
  CONSTRAINT [uniq_sso_providers_provider_id] UNIQUE ([provider_id])
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[subscriptions]', N'U') IS NULL
CREATE TABLE [subscriptions] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [plan] varchar(8000) NOT NULL,
  [reference_id] varchar(8000) NOT NULL,
  [stripe_customer_id] varchar(8000) NULL,
  [stripe_subscription_id] varchar(8000) NULL,
  [status] varchar(8000) NULL DEFAULT 'incomplete',
  [period_start] datetime2(3) NULL,
  [period_end] datetime2(3) NULL,
  [trial_start] datetime2(3) NULL,
  [trial_end] datetime2(3) NULL,
  [cancel_at_period_end] smallint NULL DEFAULT 0,
  [cancel_at] datetime2(3) NULL,
  [canceled_at] datetime2(3) NULL,
  [ended_at] datetime2(3) NULL,
  [seats] integer NULL,
  [billing_interval] varchar(8000) NULL,
  [stripe_schedule_id] varchar(8000) NULL,
  [limits] varchar(8000) NULL
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF OBJECT_ID(N'[rate_limits]', N'U') IS NULL
CREATE TABLE [rate_limits] (
  [id] varchar(255) PRIMARY KEY NOT NULL,
  [key] varchar(255) NOT NULL,
  [count] integer NOT NULL,
  [last_request] bigint NOT NULL,
  CONSTRAINT [uniq_rate_limits_key] UNIQUE ([key])
);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'uniq_users_username' AND object_id = OBJECT_ID(N'[users]')) CREATE UNIQUE INDEX [uniq_users_username] ON [users] ([username]) WHERE [username] IS NOT NULL;

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'uniq_users_phone_number' AND object_id = OBJECT_ID(N'[users]')) CREATE UNIQUE INDEX [uniq_users_phone_number] ON [users] ([phone_number]) WHERE [phone_number] IS NOT NULL;

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_sessions_on_user_id' AND object_id = OBJECT_ID(N'[sessions]')) CREATE INDEX [index_sessions_on_user_id] ON [sessions] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_accounts_on_user_id' AND object_id = OBJECT_ID(N'[accounts]')) CREATE INDEX [index_accounts_on_user_id] ON [accounts] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_verifications_on_identifier' AND object_id = OBJECT_ID(N'[verifications]')) CREATE INDEX [index_verifications_on_identifier] ON [verifications] ([identifier]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_two_factors_on_secret' AND object_id = OBJECT_ID(N'[two_factors]')) CREATE INDEX [index_two_factors_on_secret] ON [two_factors] ([secret]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_two_factors_on_user_id' AND object_id = OBJECT_ID(N'[two_factors]')) CREATE INDEX [index_two_factors_on_user_id] ON [two_factors] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_members_on_organization_id' AND object_id = OBJECT_ID(N'[members]')) CREATE INDEX [index_members_on_organization_id] ON [members] ([organization_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_members_on_user_id' AND object_id = OBJECT_ID(N'[members]')) CREATE INDEX [index_members_on_user_id] ON [members] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_invitations_on_organization_id' AND object_id = OBJECT_ID(N'[invitations]')) CREATE INDEX [index_invitations_on_organization_id] ON [invitations] ([organization_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_invitations_on_email' AND object_id = OBJECT_ID(N'[invitations]')) CREATE INDEX [index_invitations_on_email] ON [invitations] ([email]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_teams_on_organization_id' AND object_id = OBJECT_ID(N'[teams]')) CREATE INDEX [index_teams_on_organization_id] ON [teams] ([organization_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_team_members_on_team_id' AND object_id = OBJECT_ID(N'[team_members]')) CREATE INDEX [index_team_members_on_team_id] ON [team_members] ([team_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_team_members_on_user_id' AND object_id = OBJECT_ID(N'[team_members]')) CREATE INDEX [index_team_members_on_user_id] ON [team_members] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_organization_roles_on_organization_id' AND object_id = OBJECT_ID(N'[organization_roles]')) CREATE INDEX [index_organization_roles_on_organization_id] ON [organization_roles] ([organization_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_organization_roles_on_role' AND object_id = OBJECT_ID(N'[organization_roles]')) CREATE INDEX [index_organization_roles_on_role] ON [organization_roles] ([role]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_wallet_addresses_on_user_id' AND object_id = OBJECT_ID(N'[wallet_addresses]')) CREATE INDEX [index_wallet_addresses_on_user_id] ON [wallet_addresses] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_api_keys_on_config_id' AND object_id = OBJECT_ID(N'[api_keys]')) CREATE INDEX [index_api_keys_on_config_id] ON [api_keys] ([config_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_api_keys_on_key' AND object_id = OBJECT_ID(N'[api_keys]')) CREATE INDEX [index_api_keys_on_key] ON [api_keys] ([key]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_api_keys_on_reference_id' AND object_id = OBJECT_ID(N'[api_keys]')) CREATE INDEX [index_api_keys_on_reference_id] ON [api_keys] ([reference_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_passkeys_on_user_id' AND object_id = OBJECT_ID(N'[passkeys]')) CREATE INDEX [index_passkeys_on_user_id] ON [passkeys] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_clients_on_user_id' AND object_id = OBJECT_ID(N'[oauth_clients]')) CREATE INDEX [index_oauth_clients_on_user_id] ON [oauth_clients] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_clients_on_reference_id' AND object_id = OBJECT_ID(N'[oauth_clients]')) CREATE INDEX [index_oauth_clients_on_reference_id] ON [oauth_clients] ([reference_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_refresh_tokens_on_client_id' AND object_id = OBJECT_ID(N'[oauth_refresh_tokens]')) CREATE INDEX [index_oauth_refresh_tokens_on_client_id] ON [oauth_refresh_tokens] ([client_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_refresh_tokens_on_session_id' AND object_id = OBJECT_ID(N'[oauth_refresh_tokens]')) CREATE INDEX [index_oauth_refresh_tokens_on_session_id] ON [oauth_refresh_tokens] ([session_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_refresh_tokens_on_user_id' AND object_id = OBJECT_ID(N'[oauth_refresh_tokens]')) CREATE INDEX [index_oauth_refresh_tokens_on_user_id] ON [oauth_refresh_tokens] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_refresh_tokens_on_reference_id' AND object_id = OBJECT_ID(N'[oauth_refresh_tokens]')) CREATE INDEX [index_oauth_refresh_tokens_on_reference_id] ON [oauth_refresh_tokens] ([reference_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_access_tokens_on_client_id' AND object_id = OBJECT_ID(N'[oauth_access_tokens]')) CREATE INDEX [index_oauth_access_tokens_on_client_id] ON [oauth_access_tokens] ([client_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_access_tokens_on_user_id' AND object_id = OBJECT_ID(N'[oauth_access_tokens]')) CREATE INDEX [index_oauth_access_tokens_on_user_id] ON [oauth_access_tokens] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_access_tokens_on_session_id' AND object_id = OBJECT_ID(N'[oauth_access_tokens]')) CREATE INDEX [index_oauth_access_tokens_on_session_id] ON [oauth_access_tokens] ([session_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_access_tokens_on_refresh_id' AND object_id = OBJECT_ID(N'[oauth_access_tokens]')) CREATE INDEX [index_oauth_access_tokens_on_refresh_id] ON [oauth_access_tokens] ([refresh_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_consents_on_client_id' AND object_id = OBJECT_ID(N'[oauth_consents]')) CREATE INDEX [index_oauth_consents_on_client_id] ON [oauth_consents] ([client_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_consents_on_user_id' AND object_id = OBJECT_ID(N'[oauth_consents]')) CREATE INDEX [index_oauth_consents_on_user_id] ON [oauth_consents] ([user_id]);

SET ANSI_NULLS ON;
SET QUOTED_IDENTIFIER ON;
SET ANSI_WARNINGS ON;
SET ANSI_PADDING ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET ARITHABORT ON;
SET NUMERIC_ROUNDABORT OFF;
IF NOT EXISTS (SELECT name FROM sys.indexes WHERE name = 'index_oauth_consents_on_reference_id' AND object_id = OBJECT_ID(N'[oauth_consents]')) CREATE INDEX [index_oauth_consents_on_reference_id] ON [oauth_consents] ([reference_id]);
```
